spring.application.name=SkillsBarter
server.port=${SERVER_PORT:8080}

spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/Barter_It}

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS_ENABLE:true}

logging.level.org.springframework.security=${SPRING_SECURITY_LOG_LEVEL:INFO}

jwt.Secret=${JWT_SECRET:change-this-development-secret-at-least-32-chars}
jwt.expiry=${JWT_EXPIRY_MS:86400000}
